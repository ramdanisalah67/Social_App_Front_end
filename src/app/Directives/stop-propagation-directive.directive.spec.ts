import { StopPropagationDirectiveDirective } from './stop-propagation-directive.directive';

describe('StopPropagationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StopPropagationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
